name = "mystroshop-api"
main = "worker.js"
compatibility_date = "2026-09-10"

[vars]
APP_ORIGIN = "https://castorlucjulesmichel.github.io"
FIREBASE_PROJECT_ID = "mystroshop-eab92"
MONCASH_MODE = "live"

# Never commit private keys, client secrets, payout tokens, or provider credentials.
# Keep Firebase service credentials, MonCash credentials, NatCash/Paym credentials,
# and any payout-provider URL/token in Cloudflare Worker Secrets/Variables only.